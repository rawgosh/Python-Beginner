#functions related to numbers

import math #importing math module for different functions


height = float(input("Enter your height :(In cm) "))
a = 65
b = 654
c = 6948

print(round(height)) #it round the number to the nearest big or nearest small.
print(math.ceil(height)) #rounds to the nearest big integer
print(math.floor(height)) #rounds to the nearest small integer
print(abs(-height)) #it gives the absolute value
print(pow(height,2)) #raises the power of height with 2 
print(math.sqrt(height)) #calculates the square root of the supplied number
print(max(a,b,c)) #compares and finds the maximum
print(min(a,b,c)) #compares and finds the minimum




